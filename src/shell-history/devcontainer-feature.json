{
  "name": "Shell History",
  "id": "shell-history",
  "version": "0.0.1",
  "description": "Persist shell history across dev container instances.",
  "mounts": [
    {
      "source": "${devcontainerId}-shellhistory",
      "target": "/shellhistory",
      "type": "volume"
    }
  ]
}
